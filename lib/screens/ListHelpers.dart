import 'package:flutter/material.dart';

class ListHelpers extends StatelessWidget {
  const ListHelpers({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Row(
        children: [Text("List Of summaries")],
      ),
    );
  }
}
